# Public backend origin only. Never place provider keys in a Vite environment file.
VITE_API_BASE_URL=http://localhost:3000/api
# Alternatively set only the public Express origin; the frontend appends /api.
# VITE_API_ORIGIN=https://recipe-match-backend.example.com
